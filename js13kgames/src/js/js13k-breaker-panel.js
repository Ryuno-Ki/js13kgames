(function(global) {
    "use strict";
    var app, element, schalter;

    // Shorthands
    app = global.JS13KBP;
    element = app.element;

    schalter = new element.ElectronicElement("schalter");
})(this);
