(function(global) {
    "use strict";
    var app, element, switchElement;

    // Shorthands
    app = global.JS13KBP;
    element = app.element;

    switchElement = new element.SwitchElement("switchElement");
})(this);
